# Copy these non-secret variable names to ../.env and set the real values there.
PORT=3001
NODE_ENV=development

# Server-only database credentials. Never expose SUPABASE_SERVICE_ROLE_KEY in frontend code.
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

